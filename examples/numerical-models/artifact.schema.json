{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "numerical-model-vv-artifact",
  "type": "object",
  "required": ["slug", "source", "values"],
  "properties": {
    "slug": { "type": "string" },
    "source": { "enum": ["python", "rust"] },
    "values": { "type": "object" },
    "series": { "type": "object" }
  }
}
